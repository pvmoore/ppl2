
import core.intrinsics
import test_imports

public

struct M1 = [
    //foo = {
    //    return 3
    //}
    foo = <T> { T a ->
        return 4
    }
]

testTemplateFunctions = {
    globals = {
        // templated function
        assert 1 == func<int>(3)
        assert 1 == func<float>(3.1)
        assert 3 == func<int,double>(1,2.2)
        // untemplated function
        assert 2 == func(3.2)

        // imported templated function
        assert 10 == tfunc<bool>(true)
        assert 30 == tfunc<double,float>(1,2)
        // imported untemplated fucntion
        assert 20 == tfunc(3.1)
    }
    closures = {

    }
    structs = {

    }
    globals()
    closures()
    structs()

    //M1 m
}

func = <T> { T a ->
    return 1
}
func = <A,B> { A a, B b ->
    return 3
}
func = { float a ->
    return 2
}
