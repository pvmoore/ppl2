
public

println = { string s ->
    // Can we assume string will
    // have a trailing zero?
    puts(s.ptr)
}
/// todo - don't emit a trailing newline
print = { int n ->
    [:byte 20] bytes
    _itoa(n, bytes.ptr, 10)

    puts(bytes.ptr)
}
