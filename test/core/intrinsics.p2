
import core.c
import core.string
import core.console

public

__assert = { bool result, string moduleName, int line ->
    if(not result) {
        println("Assertion failed in module ")
        println(moduleName)
        println("on line (todo)")
        exit(-1)
    }
}
