
public

__assert = { bool result, byte* moduleName, int line ->
    if(not result) {
        println("Assertion failed in module ")
        puts(moduleName)
        println("on line")
        print(line)
        exit(-1)
    }
}
