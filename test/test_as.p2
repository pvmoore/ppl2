 
public

testAs {
    var a = 10 as byte; assert a is byte

    var f = 10.3d as int; assert f is int and f==10

    // tuples
    alias Tuple = [int,byte,short]
    var b  = [1,2 as byte,3 as short] as [int,byte,short]
    var b2 = [1,2 as byte,3 as short] as Tuple //struct 

    struct Name { int a }
    var c = null as Name*

    
}
