
import test_imports2

public

bool isit = false

struct Dog = [
    int age
]
struct Cat = [
    bool isGinger
]
struct Bat = [
    float size
]
struct Pig = [
    int weight
]
struct Wolf = [
    bool isGrey
]
struct Rose = <T> [
    T a
]

define AnInt = int
define IntPtrPtr = IntPtr*
define ANiceRose = OrangeRose
define BlueRose = RedRose<bool>

importedFunc = {

}

// global function templates
tfunc = <T> { T a ->
    return 10
}
tfunc = <A,B> { A a, B b ->
    return 30
}
tfunc = { float f ->
    return 20
}
struct M3 = <T> [
    T t
    foo = { T a ->
        return 70
    }
    foo = <A> { A a, T t ->
        return 71
    }
    foo = <A,B> { A a, B b ->
        return 72
    }
]
