
import glfw::common

WinMain {  
    
    var file = fopen("myfile.txt".cstr(), "w".cstr())
    fputs("blah\n".cstr(), file)
    fclose(file)  
    
    if(int r = glfwInit(); r) {
        glfwTerminate() 
    }
    
    //int major;
    //int minor;
    //int rev;
    //glfwGetVersion(&major, &minor, &rev)

    println("Hello")
}

